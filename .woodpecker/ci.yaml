# self-hosted CI refresh after agent scaling
when:
  - event: [push, pull_request]

steps:
  - name: gradle
    image: eclipse-temurin:21-jdk
    commands:
      - chmod +x gradlew
      - ./gradlew --no-daemon build
